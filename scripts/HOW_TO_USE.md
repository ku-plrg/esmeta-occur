# How-to-run

1. Run `./gen_provenance_heatmaps.sh`
2. See `../logs/analyze/heatmap_depth_leafcnt.pdf` and two more

## Troubleshooting

If python code does not run, please install the required packages:

```bash
pip install pandas numpy matplotlib seaborn
```
```
